/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['8app81mbi2.execute-api.ap-northeast-1.amazonaws.com']
  }
}

module.exports = nextConfig
